# 工作空间移动和 Git 配置指南

## 📋 任务清单

- [x] 安装 PowerShell 7 ✅
- [x] 配置 UTF-8 编码 ✅
- [ ] 移动工作空间到 `G:\aitogod\AIWorkspace`
- [ ] 配置 Git 远程仓库
- [ ] 关联到 GitHub 仓库

---

## 🚀 步骤 1：移动工作空间（手动操作）

### ⚠️ 重要：由于 Trae IDE 沙盒限制，需要手动操作

1. **关闭 Trae IDE**
   - 保存所有文件
   - 完全关闭 Trae IDE

2. **打开 Windows 资源管理器**
   - 按 `Win + E`

3. **导航到源目录**
   - 地址栏输入：`G:\aitogod\Trae CN\TareWorkspace`

4. **全选并剪切**
   - 按 `Ctrl + A` 全选
   - 按 `Ctrl + X` 剪切

5. **导航到目标目录**
   - 地址栏输入：`G:\aitogod\AIWorkspace`

6. **粘贴**
   - 按 `Ctrl + V` 粘贴
   - 等待复制完成

7. **验证移动成功**
   - 检查 `G:\aitogod\AIWorkspace\ai-agent-lab` 是否存在
   - 检查文件是否完整

---

## 🔧 步骤 2：重新打开 Trae IDE

1. **打开 Trae IDE**
2. **打开新工作空间**
   - 文件 → 打开文件夹
   - 选择：`G:\aitogod\AIWorkspace\ai-agent-lab`

---

##  步骤 3：配置 Git 远程仓库

打开 Trae IDE 终端，执行以下命令：

### 3.1 检查当前 Git 状态

```powershell
cd "G:\aitogod\AIWorkspace\ai-agent-lab"
git remote -v
```

### 3.2 添加/修改远程仓库

```powershell
# 如果已有远程 origin，先删除
git remote remove origin

# 添加新的远程仓库
git remote add origin https://github.com/1141435784-jinzi/repository-ai-agent.git

# 验证
git remote -v
```

### 3.3 拉取远程仓库信息

```powershell
git fetch origin
```

### 3.4 设置上游分支

```powershell
# 查看本地分支
git branch

# 设置当前分支的上游
git branch --set-upstream-to=origin/main main

# 或者如果是 master 分支
git branch --set-upstream-to=origin/master master
```

### 3.5 同步最新代码

```powershell
git pull origin main
```

---

## 📝 步骤 4：验证配置

### 检查 Git 状态

```powershell
git status
git log --oneline -5
```

### 测试推送（可选）

```powershell
# 如果有本地更改，先提交
git add .
git commit -m "feat: 工作空间迁移"

# 推送到远程
git push origin main
```

---

## 🛠️ 常见问题

### 问题 1：移动后 Git 不工作

**解决方法**：
```powershell
# 重新初始化 Git（保留.git 目录）
cd "G:\aitogod\AIWorkspace\agent-lab4Tare"
git status

# 如果报错，重新设置远程
git remote remove origin
git remote add origin https://github.com/1141435784-jinzi/repository-ai-agent.git
```

### 问题 2：权限错误

**解决方法**：
- 确保以管理员身份运行 Trae IDE
- 或者检查文件夹权限

### 问题 3：远程仓库冲突

**解决方法**：
```powershell
# 强制拉取远程仓库
git fetch --all
git reset --hard origin/main
```

---

## ✅ 完成检查清单

- [ ] 工作空间已移动到 `G:\aitogod\AIWorkspace\ai-agent-lab`
- [ ] Trae IDE 已重新打开新工作空间
- [ ] Git 远程仓库已配置
- [ ] 可以执行 `git status` 查看状态
- [ ] 可以执行 `git pull` 拉取代码
- [ ] 可以执行 `git push` 推送代码

---

## 📞 需要帮助？

移动完成后，告诉我，我会帮您：
1. ✅ 配置 Git 远程仓库
2. ✅ 验证仓库关联
3. ✅ 测试推送功能

---

**GitHub 仓库**: https://github.com/1141435784-jinzi/repository-ai-agent  
**本地路径**: `G:\aitogod\AIWorkspace\ai-agent-lab`
